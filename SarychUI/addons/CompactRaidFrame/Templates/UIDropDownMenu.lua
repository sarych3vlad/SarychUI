-- CompactRaidFrame dropdown visuals are applied via SarychUI owner guard (options_widgets.lua).
-- This file is kept as a load-order stub so embeds.xml does not need changes.

if SarychUI and SarychUI.InstallDropdownListSkinGuard then
	SarychUI:InstallDropdownListSkinGuard()
end
